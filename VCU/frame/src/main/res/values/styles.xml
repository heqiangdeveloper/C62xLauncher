<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- dialog animation -->
    <style name="common_frame_dialog_animation" parent="android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/common_frame_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/common_frame_dialog_out</item>
    </style>

    <!-- dialog style -->
    <style name="common_frame_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/common_frame_dialog_animation</item>
    </style>

    <!-- progress dialog style -->
    <style name="_progress_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/common_frame_dialog_animation</item>
        <item name="android:progressBarStyle">@android:style/Widget.ProgressBar.Horizontal</item>
    </style>
</resources>